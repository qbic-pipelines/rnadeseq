{
    "$schema": "http://json-schema.org/draft-07/schema",
    "$id": "https://raw.githubusercontent.com/nf-core/rnadeseq/master/nextflow_schema.json",
    "title": "nf-core/rnadeseq pipeline parameters",
    "description": "Differential gene expression analysis and pathway analysis of RNAseq data",
    "type": "object",
    "definitions": {
        "mandatory_arguments": {
            "title": "Mandatory arguments",
            "type": "object",
            "description": "",
            "default": "",
            "properties": {
                "rawcounts": {
                    "type": "string",
                    "description": "Raw count table (TSV). Columns are samples and rows are genes. 1st column Ensembl_ID, 2nd column gene_name."
                },
                "metadata": {
                    "type": "string",
                    "description": "Metadata table (TSV). Rows are samples and columns contain sample grouping."
                },
                "model": {
                    "type": "string",
                    "description": "Linear model function to calculate the contrasts (TXT). Variable names should be columns in metadata file."
                },
                "species": {
                    "type": "string",
                    "description": "Species name. Format example: Hsapiens."
                },
                "project_summary": {
                    "type": "string",
                    "description": "Project summary file downloaded from the qPortal."
                },
                "versions": {
                    "type": "string",
                    "description": "Software_versions.csv generated by the RNAseq pipeline."
                },
                "report_options": {
                    "type": "string",
                    "description": "Configuration file containing the section to be present in the report. Also contains any line to be added to the outlook."
                }
            },
            "required": [
                "rawcounts",
                "metadata",
                "model",
                "species",
                "project_summary",
                "versions",
                "report_options",
                "profile"
            ]
        },
        "new_group_2": {
            "title": "New Group 2",
            "type": "object",
            "description": "",
            "default": "",
            "properties": {
                "contrast_matrix": {
                    "type": "string",
                    "description": "Tsv indicating which contrasts to consider, one contrast per column. 1 or 0 for every coefficient of the linear model. Check contrasts docs."
                },
                "contrast_list": {
                    "type": "string",
                    "description": "Tsv indicating list of the contrasts to calculate. 3 columns: factor name, contrast numerator and denominator. Check contrasts docs."
                },
                "contrast_pairs": {
                    "type": "string",
                    "description": "Tsv indicating list of contrast pairs to calculate. 3 columns: contrast name, numerator and denominator. Check contrasts docs."
                },
                "relevel": {
                    "type": "string",
                    "description": "Tsv indicating list of factors (conditions in the metadata table) and the new level on which to relevel the factor. Check contrasts docs."
                },
                "logFCthreshold": {
                    "type": "integer",
                    "description": "Threshold (int) to apply to Log 2 Fold Change to consider a gene as differentially expressed.",
                    "default": 0
                },
                "genelist": {
                    "type": "string",
                    "description": "Txt file with list of genes (one per line) of which to plot heatmaps for normalized counts across all samples."
                },
                "batch_effect": {
                    "type": "boolean",
                    "description": "Turn on this flag if you wish to consider batch effects. You need to add the batch effect to the linear model too!"
                },
                "quote": {
                    "type": "string",
                    "description": "Signed copy of the offer.",
                    "fa_icon": "fas fa-file-signature"
                },
                "kegg_blacklist": {
                    "type": "string",
                    "description": "Txt file with list of pathways (one per line) that should be discarded for the KEGG pathway plotting (e.g. because the xml file in KEGG contains errors)."
                },
                "min_DEG_pathway": {
                    "type": "integer",
                    "description": "Integer indicating how many genes should be DE in a pathway for this pathway to be considered enriched",
                    "default": 1
                }
            }
        },
        "other_options": {
            "title": "Other options",
            "type": "object",
            "default": ""
        }
    },
    "allOf": [
        {
            "$ref": "#/definitions/mandatory_arguments"
        },
        {
            "$ref": "#/definitions/new_group_2"
        },
        {
            "$ref": "#/definitions/other_options"
        }
    ],
    "properties": {
        "help": {
            "type": "boolean",
            "description": "Display help text.",
            "fa_icon": "fas fa-question-circle"
        }
    }
}