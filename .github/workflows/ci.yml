name: nf-core CI
# This workflow is triggered on pushes and PRs to the repository.
# It runs the pipeline with the minimal test dataset to check that it completes without any syntax errors
on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        nxf_ver: ['19.10.0', '']
    steps:
      - uses: actions/checkout@v1
      - name: Install Nextflow
        run: |
          export NXF_VER=${{ matrix.nxf_ver }}
          wget -qO- get.nextflow.io | bash
          sudo mv nextflow /usr/local/bin/
      - name: Pull docker image
        run: |
          docker pull qbicpipelines/rnadeseq:dev
          docker tag qbicpipelines/rnadeseq:dev qbicpipelines/rnadeseq:dev
      - name: Basic workflow tests
        run: nextflow run ${GITHUB_WORKSPACE} -profile test,docker
  contrast_matrix:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        nxf_ver: ['19.10.0']
    steps:
      - uses: actions/checkout@v1
      - name: Install Nextflow
        run: |
          export NXF_VER=${{ matrix.nxf_ver }}
          wget -qO- get.nextflow.io | bash
          sudo mv nextflow /usr/local/bin/
      - name: Pull docker image
        run: |
          docker pull qbicpipelines/rnadeseq:dev
          docker tag qbicpipelines/rnadeseq:dev qbicpipelines/rnadeseq:dev
      - name: Contrast matrix test
        run: nextflow run ${GITHUB_WORKSPACE} -profile test_contrast_matrix,docker
  contrast_list:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        nxf_ver: ['19.10.0']
    steps:
      - uses: actions/checkout@v1
      - name: Install Nextflow
        run: |
          export NXF_VER=${{ matrix.nxf_ver }}
          wget -qO- get.nextflow.io | bash
          sudo mv nextflow /usr/local/bin/
      - name: Pull docker image
        run: |
          docker pull qbicpipelines/rnadeseq:dev
          docker tag qbicpipelines/rnadeseq:dev qbicpipelines/rnadeseq:dev
      - name: Contrast list test
        run: nextflow run ${GITHUB_WORKSPACE} -profile test_contrast_list,docker