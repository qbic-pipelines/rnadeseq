name: Docker push
# This builds the docker image and pushes it to DockerHub
# Runs on qbic-pipelines repo releases and push event to 'dev' branch (PR merges)
on:
  push:
    branches:
      - dev
  release:
    types: [published]

jobs:
  push_dockerhub:
    name: Push new Docker image to Docker Hub
    runs-on: ubuntu-latest
    # Only run for the qbicpipelines repo, for releases and merged PRs
    if: ${{ github.repository == 'qbic-pipelines/rnadeseq' }}
    steps:
      - name: Check out pipeline code
        uses: actions/checkout@v2

      - name: Build new docker image
        run: docker build --no-cache . -t ghcr.io/qbicpipelines/rnadeseq:latest

      - name: Log in to registry
        # Update the personal access token to GITHUB_TOKEN
        run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u $ --password-stdin

      - name: Push image to GHCR (dev)
        if: ${{ github.event_name == 'push' }}
        run: |
          docker tag ghcr.io/qbicpipelines/rnadeseq:latest ghcr.io/qbicpipelines/rnadeseq:dev
          docker push ghcr.io/qbicpipelines/rnadeseq:dev

      - name: Push image to GHCR (release)
        if: ${{ github.event_name == 'release' }}
        run: |
          docker tag ghcr.io/qbicpipelines/rnadeseq:latest ghcr.io/qbicpipelines/rnadeseq:${{ github.event.release.tag_name }}
          docker push ghcr.io/qbicpipelines/rnadeseq:${{ github.event.release.tag_name }}
